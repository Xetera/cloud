apiVersion: v1
kind: Service
metadata:
  name: weblate-redis
  namespace: default
spec:
  selector:
    app: weblate-redis
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: tcp
