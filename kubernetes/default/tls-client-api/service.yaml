apiVersion: v1
kind: Service
metadata:
  name: tls-client-api
  namespace: default
spec:
  selector:
    app: tls-client-api
  ports:
    - port: 8080
      targetPort: 8080
    - port: 8090
      targetPort: 8090
