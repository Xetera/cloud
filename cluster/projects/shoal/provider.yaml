apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: shoal
  namespace: flux-system
spec:
  type: github
  address: https://github.com/xetera/shoal
  secretRef:
    name: github-pat
