apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: placewaifu
  namespace: flux-system
spec:
  type: github
  address: https://github.com/Xetera/placewaifu
  secretRef:
    name: github-pat
