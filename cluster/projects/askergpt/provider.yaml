apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: askergpt
  namespace: flux-system
spec:
  type: github
  address: https://github.com/Xetera/askergpt
  secretRef:
    name: github-pat
